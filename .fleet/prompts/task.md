# Task: {{issue.key}} — {{issue.summary}}

Repository: `{{repo}}`
Source: {{source}}
Issue type: {{issue.type}}
Labels: {{issue.labels}}
Issue URL: {{issue.url}}

## Description

{{issue.description}}

## Instructions

- This is the Flutter/Dart project `cut_in_half` (a precision party game).
- Make the smallest coherent change that fully addresses the issue.
- Follow existing conventions in `lib/`; keep widget/unit tests under `test/`.
- Add or update tests when the change warrants it.
- Run `flutter analyze` and `flutter test` before finishing.
- Do not edit `.fleet/`, `.github/`, `backlog/`, or `backlog-to-pr/`.
