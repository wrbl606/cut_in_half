{
  "name": "cut_in_half",
  "version": "0.0.0",
  "private": true,
  "description": "Shared tooling for cut_in_half. Pinned devDependencies ensure the same CLI versions across devices.",
  "scripts": {
    "backlog": "backlog"
  },
  "devDependencies": {
    "backlog.md": "1.47.1"
  }
}
