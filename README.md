# cut_in_half

A precision party game — cut the object into equal pieces.

## Getting started

Install dependencies and run the app:

```sh
flutter pub get
flutter run
```

## Testing

Run the test suite:

```sh
flutter test
```
